# 中国十年教育数据可视化系统 —— 项目答辩报告

## 一、项目概述

本项目是一个面向“中国十年教育数据”的可视化分析系统，核心目标是把分散在 CSV / MySQL 中的教育指标（在校生数、招生数、毕业生数、教育经费、学校数等）清洗、入库后，通过 Web 前端以直观、可交互的图表方式呈现，并提供基于历史数据的趋势预测能力。

系统采用**前后端分离**架构：

- **前端**：Vue 3 + Vue Router + ECharts + Vite
- **Node 中间层**：Express（负责认证、MySQL 查询、代理到 Python 分析服务）
- **分析服务**：FastAPI + scikit-learn（趋势预测）
- **数据层**：MySQL（`cn_edu_viz` 库）

---

## 二、前端技术栈与项目结构

### 2.1 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | Vue 3（Composition API / `<script setup>`） | 组件级响应式、逻辑聚合 |
| 路由 | Vue Router 4 | 前端路由 + 登录守卫 |
| 图表 | ECharts 5 | 折线、柱状、饼图、地图、堆叠等 |
| 构建 | Vite 6 | 开发服务器、代理、热更新 |
| 状态 | 组合式函数 `useAuth`（Pinia 未引入） | JWT + localStorage 轻量方案 |

依赖声明见 [`my-vue/package.json`](file:///d:/Projects/cn-edu-viz/my-vue/package.json)：

```json
{
  "dependencies": {
    "echarts": "^6.1.0",
    "vue": "^3.5.38",
    "vue-router": "^4.6.4"
  }
}
```

### 2.2 前端目录结构

```
my-vue/
├── public/
├── src/
│   ├── components/          # 可复用组件
│   │   ├── DataChart.vue    # 通用折线/柱状图
│   │   ├── DataTable.vue    # 通用表格
│   │   └── TableNav.vue     # 表名标签导航
│   ├── router/index.js      # 路由与登录守卫
│   ├── stores/auth.js       # JWT 认证状态
│   ├── utils/echarts-config.js  # 图表公共配置
│   ├── views/
│   │   ├── Login.vue            # 登录/注册
│   │   ├── Dashboard.vue        # 数据可视化入口
│   │   ├── TrendPrediction.vue  # 趋势预测
│   │   └── dashboard/           # Dashboard 的四个子面板
│   │       ├── OverviewPanel.vue
│   │       ├── NationalTrendPanel.vue
│   │       ├── ProvincialComparePanel.vue
│   │       └── StructureAnalysisPanel.vue
│   ├── App.vue
│   └── main.js
└── vite.config.js
```

---

## 三、全局基础设施

### 3.1 应用入口 [`main.js`](file:///d:/Projects/cn-edu-viz/my-vue/src/main.js)

```js
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

const app = createApp(App)
app.use(router)
app.mount('#app')
```

仅做三件事：创建 Vue 实例、挂载路由、挂载到 `#app`。

### 3.2 布局组件 [`App.vue`](file:///d:/Projects/cn-edu-viz/my-vue/src/App.vue)

`App.vue` 是整个应用的“壳”，包含：

1. **顶部导航栏**：Logo `EDU·VIZ`、两个主导航（数据可视化、趋势预测）、当前登录用户与退出按钮。
2. **主内容区**：`<router-view />` 承载各页面。
3. **底部页脚**：项目标题。
4. **全局公共样式**：定义了 `.hero`、`.subnav`、`.year-nav`、`.grid-2`、`.chart`、`.chart-wrap`、`.error-msg` 等样式类，供各子页面复用。

核心导航渲染逻辑：

```vue
<script setup>
import { useRoute } from 'vue-router'
import { useAuth } from './stores/auth'

const route = useRoute()
const { username, isLoggedIn, logout } = useAuth()

const navItems = [
  { path: '/dashboard', label: '数据可视化' },
  { path: '/trend-prediction', label: '趋势预测' },
]
</script>

<template>
  <header class="brand">
    <div class="brand-inner">
      <div class="brand-logo">EDU·VIZ</div>
      <div class="brand-actions">
        <nav class="main-nav">
          <router-link
            v-for="n in navItems"
            :key="n.path"
            :to="n.path"
            :class="['nav-link', { active: route.path === n.path || route.path.startsWith(n.path + '/') }]"
          >{{ n.label }}</router-link>
        </nav>
        <div class="nav-right">
          <template v-if="isLoggedIn">
            <span class="user-name">{{ username }}</span>
            <button class="logout-btn" @click="logout">退出</button>
          </template>
        </div>
      </div>
    </div>
  </header>

  <main id="main-content" class="page">
    <router-view />
  </main>
</template>
```

高亮点：

- 使用 `useRoute` 获取当前路径，动态添加 `.active` 类。
- 使用 `useAuth` 读取登录态与用户名，未登录时不显示用户信息（登录页本身也无导航用户信息）。
- 公共样式下沉到 `App.vue`，各页面只需关注内容结构。

### 3.3 路由与登录守卫 [`router/index.js`](file:///d:/Projects/cn-edu-viz/my-vue/src/router/index.js)

```js
import { createRouter, createWebHistory } from 'vue-router'
import Dashboard from '../views/Dashboard.vue'
import TrendPrediction from '../views/TrendPrediction.vue'

const routes = [
  { path: '/', redirect: '/dashboard' },
  { path: '/dashboard', name: 'Dashboard', component: Dashboard },
  // 旧路由重定向到 /dashboard
  { path: '/overview', redirect: '/dashboard' },
  { path: '/national-trend', redirect: '/dashboard' },
  { path: '/provincial-compare', redirect: '/dashboard' },
  { path: '/structure-analysis', redirect: '/dashboard' },
  { path: '/trend-prediction', name: 'TrendPrediction', component: TrendPrediction },
  {
    path: '/login',
    name: 'Login',
    component: () => import('../views/Login.vue'),
    meta: { public: true, guestOnly: true },
  },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

router.beforeEach((to) => {
  const token = localStorage.getItem('token')
  const isLoggedIn = !!token

  if (!isLoggedIn && !to.meta.public) {
    return { path: '/login', query: { redirect: to.fullPath } }
  }

  if (isLoggedIn && to.meta.guestOnly) {
    return { path: '/dashboard' }
  }

  return true
})

export default router
```

路由设计要点：

- 默认首页重定向到 `/dashboard`。
- 旧页面路径（`/overview` 等）统一重定向到 `/dashboard`，避免收藏夹失效。
- `/login` 是懒加载的公共页面，且**已登录用户不可访问**（`guestOnly`）。
- `router.beforeEach` 全局守卫：无 token 且访问非公共页时，跳登录并携带 `redirect`；已登录访问登录页时，跳首页。

### 3.4 认证状态 [`stores/auth.js`](file:///d:/Projects/cn-edu-viz/my-vue/src/stores/auth.js)
